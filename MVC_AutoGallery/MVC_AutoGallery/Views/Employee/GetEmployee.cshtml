@model MVC_AutoGallery.Models.Entity.Tbl_Employee
@{
    ViewBag.Title = "GetEmployee";
    Layout = "~/Views/Shared/_MainLayout.cshtml";
}

@using (Html.BeginForm("Update", "Employee", FormMethod.Post))
{
    <div class="form-group">
        @Html.LabelFor(m => m.Employee_Id)
        @Html.TextBoxFor(m => m.Employee_Id, new {@class = "form-control"})
    </div>
}
